# URL du backend (laisser vide en dev — le proxy Vite est utilisé automatiquement)
# En production, mettre l'URL complète du backend déployé
VITE_API_URL=

# URL publique de ce frontend admin (utilisé pour les liens absolus, canonical, partage)
VITE_APP_URL=
